module.exports.testCases = {
    // Each test case property name corresponds to a file in ./challenges folder
    char_count: [
        [['aaa', 'a'], 1],
        [['abc', 'c'], 0],
        [['abb', 'b'], 2],
    ]
}